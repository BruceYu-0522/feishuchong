DevFlow Pencil Sketch
=====================

用途：
这个文件代表方案设计阶段的 Pencil 草图产物。当前 Codex 环境里的 Pencil 截图渲染没有稳定输出节点内容，所以这里先以可追踪的草图说明文件落库，DevFlow 页面会在方案设计产物中展示它的路径。

草图主题：
给任务管理系统增加按优先级筛选任务的功能。

Blueprint:

[Task 数据结构]
新增 priority 字段：high / medium / low
        |
        v
[筛选控件]
全部 / 高 / 中 / 低 segmented filter
        |
        v
[列表过滤]
按 priorityFilter 过滤任务集合
        |
        v
[空状态]
无匹配任务时提示，并提供重置入口

审批关注点：
- 筛选条件需要和搜索、排序共存。
- priority 字段需要默认值，避免旧数据异常。
